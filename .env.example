# Local configuration. Fill only the providers you use. Never commit .env.
DEEPL_API_KEY="" # Configure locally when needed.
GOOGLE_APPLICATION_CREDENTIALS="" # Configure locally when needed.
OPENAI_API_KEY="" # Configure locally when needed.
NGROK_AUTHTOKEN="" # Configure locally when needed.
